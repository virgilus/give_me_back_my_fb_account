# Complete the file, rename the file "credentials.yaml" and move it to a folder named "secrets"
# located in the root dir

# Twitter account

API_KEY:
API_KEY_SECRET:
BEARER_TOKEN:
ACCESS_TOKEN:
ACCESS_TOKEN_SECRET:

# APP settings:

CLIENT_ID:
CLIENT_SECRET:

# MISTRAL:

MISTRAL_KEY:
